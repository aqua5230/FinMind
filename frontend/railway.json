{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx next start -p $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
